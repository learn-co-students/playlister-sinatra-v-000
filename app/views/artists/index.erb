<!DOCTYPE html>
<html>
  <head>
  <title>Artists</title>
  </head>
    <body>
      <h1>List of Artists</h1>

      <br></br>

      <h3> Click on an Artist to see songs and genres</h3>
      <ul>
        <% @artists.each do |artist| %>
         <li><a href="/artists/<%= artist.slug %>"><%=artist.name%></a></li>
        <% end %>
      </ul>
    </body>
</html>