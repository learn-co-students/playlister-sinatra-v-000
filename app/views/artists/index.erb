<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <title>List of Artists</title>
  </head>
  <body>
    <ul>
      <% @artists.each do |artist| %>
        <li>
          <a href="/artists/<%= artist.slug %>"><%= artist.name %> </a>
        </li>
      <% end %>
    </ul>
  </body>
</html>
