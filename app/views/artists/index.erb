<!DOCTYPE html>
<html>
  <head>
    <title>All Artists</title>
  </head>

  <body>
  <% @artists.each do |artist| %>
    <li> <a href="/artists/<%= artist.slug %>"> <%= artist.name %> </a></li>
  <% end %>
  </body>
</html>
