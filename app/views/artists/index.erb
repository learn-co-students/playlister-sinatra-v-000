<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Artists Index Page</title>
</head>
<body>
  <h1>Artists</h1>
  <% Artist.all.each_with_index do |artist, i| %>
  <%= "#{i+1}. "%> <a href="/artists/<%=artist.slug%>"><%="#{artist.name}" %></a><br>
  <% end %>
</body>
</html>