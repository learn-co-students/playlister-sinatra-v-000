<h1>Artist: <%= @artist.name %></h1>

<h2>Songs:</h2>
  <% @artist.songs.each do |song| %>
    <a href="/songs/<%= song.slug %>"></a><%= song.name %>
  <% end %>

<h2>Genres:</h2>
  <% @artist.genres.each do |genre| %>
    <a href="/genres/<%= genre.slug %>"></a><%= genre.name %>
  <% end %>
