
<%= @artist.name %>'s songs and genres:

Songs:
<% @artist.songs.each do |song| %>
  <a href='/songs/<%=song.slug%>'><%=song.name%></a>
<%end%>

Genres:
<% @artist.genres.each do |genre| %>
  <a href='/genres/<%=genre.slug%>'><%=genre.name%></a>
<%end%>
