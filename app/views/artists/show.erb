<h1><%= @artist.name %></h1>
<h2>Songs:</h2>
  <% @artist.songs.each do |song| %>
  <li><%=  song.name %></li>
  <% end %>
<h2>Genres:</h2>
  <% @artist.genres.each do |genre| %>
  <li><%=  genre.name %></li>
  <% end %>