<ul>
  <%@artist.songs.each do |song|%>
    <li><%=song.name%></li>
    <a href="/songs/<%= song.slug %>"></a>
    <a href="/genres/<%= song.genres[0].slug %>"></a>
  <%end%>
</ul>
<li><%=@artist.genres[0].name %></li>
