<h1><%=@artist.name%></h1>
<h4>Songs:</h4>
<ul>
  <% @artist.songs.each do |song|%>
  <a href="/songs/<%= song.slug %>">
  <%= song.name %></a>
  <% end %>
</ul>

<h4>Genres:</h4>
<ul>
  <% @artist.genres.each do |genre|%>
  <a href="/genres/<%= genre.slug %>">
  <%= genre.name %></a>
  <% end %>
</ul>
