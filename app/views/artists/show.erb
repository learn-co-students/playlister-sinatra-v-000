<h1>Artist: <%=@artist.name%></h1>

<h1>Song(s): <%@artist.songs.each do |song|%>
  <a href="/songs/<%=song.slug %>"><%= song.name %></a></h1>
  <%end%>
<h1>Genre(s): <%@artist.genres.each do |genre|%>
  <a href="/genres/<%=genre.slug %>"><%= genre.name %></a></h1>
  <%end%>
  