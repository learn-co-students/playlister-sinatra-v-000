<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <h1><%= @artist.name %></h1>
    <h3>Songs </h3>
    <a href="/songs/<%= @song.slug %>"><%= @song.name %></a>
    <h3>Genre</h3>
    <a href="/genres/<%= @genre.slug %>"><%= @genre.name %></a>
  </body>
</html>
