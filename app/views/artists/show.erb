<h2>Artist: <%=@artist.name%></h2>
<%@artist.songs.each do |song|%>
  <div>
    <h4>Song: <a href="/songs/<%=song.slug%>"><%=song.name%></a></h4>
    <h4>Genre: <a href="/genres/<%=song.genres[0].slug%>"><%=song.genres[0].name.split.map { |i| i.capitalize }.join(' ')%></a></h4>
  </div>
<%end%>
