<h1>Artist info</h1>
Artist Name :<%=@artist.name%>
<br>
<ul>
Songs:<%@artist.songs.each{|song|%>
    <li><a href="/songs/<%=song.slug %>"><%= song.name %></a></li>
  <% } %>
</ul>
  <ul>
  <h5>Genres:</h5>
   <% @artist.genres.each { |genre| %>
   <li><a href="/genres/<%=genre.slug %>"><%= genre.name %></a></li>
   <% } %>
</ul>
