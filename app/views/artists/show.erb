<%= @artist.name %>
<% @artist.songs.all.each do |song| %>
<h4>Song/s: <a href='/songs/<%= song.slug %>'> <%= song.name %> </a> </h4>
<% end %>
<% @artist.songs.all.each do |song| %>
    <% song.genres.each do |genre| %>
<h4>Genre/s: <a href='/genres/<%= genre.slug %>'> <%= genre.name %> </a> </h4>
 <% end %>
<% end %>


