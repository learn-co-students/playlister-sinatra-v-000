<p>Artist Name: <%= @artist.name %></p>
<p>Songs - Genre:
  <ul>
    <% @artist.songs.each do |song| %>
    <li><a href='/songs/<%=song.slug%>'><%= song.name %></a> -
    <a href='/genres/<%=song.genres.first.slug%>'><%= song.genres.first.name %></a></li>
    <% end %>
  </ul>
</p>
