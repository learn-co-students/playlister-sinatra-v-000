<h1><%= @artist.name %></h1>

<h2>Song(s): 
<% @songs.each do |song| %>
     <a href= <%= "/songs/#{song.slug}" %> > <%= song.name %></a>
<% end  %>
</h2>

<h2>Genre(s): 
<% @genres.each do |genre| %>
     <a href= <%= "/genres/#{genre.slug}" %> > <%= genre.name %></a>
<% end  %>
</h2>

