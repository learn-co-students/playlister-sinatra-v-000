<h1>Artist Name: <%= @artist.name %></h1>
<% @artist.songs.each do |song| %>
<h2>Song Title(s): <a href="/songs/<%= song.slug %>"><%= song.name %></a></h2>
<% end %>
<% @artist.genres.each do |genre| %>
<h2>Genre Name(s): <a href="/genres/<%= genre.slug %>"><%= genre.name %></a></h2>
<% end %>