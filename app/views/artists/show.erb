<h1><%= @artist.name %></h1>
<ol> <%= @artist.name %>: Songs:
<% @artist.songs.each do |song| %>
  <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
<% end %>
</ol>
<ol><%= @artist.name %>: Genres:
  <% @artist.genres.each do |genre| %>
    <li><a href='/genres/<%= genre.slug %>'><%= genre.name %></li>
  <% end %>
</ol>
