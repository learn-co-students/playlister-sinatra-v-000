<h1><%= @artist.name %></h1>

<% @artist.songs.each do |song| %>
  Song: <a href="/songs/<%= song.slug %>"><%= song.name %><br>
<% end %>

<% @artist.genres.each do |genre| %>
  Genre: <a href="/genres/<%= genre.slug %>"><%= genre.name %><br>
<% end %>
