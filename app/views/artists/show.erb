<h1>Name: <%=@artist.name%></h1>
<h2>Songs:</h2>
<ul>
  <%@songs.each do |song|%>
    <li> <a href="/songs/<%= song.slug %>"> <%=song.name%> </a> </li>
    <%end%>
</ul>
<h2>Genres: (<%=@genres%>)</h2>

<ul>
  <%@songs.each do |song|%>
  <li><%= song.genres %></li>
</ul>
