<h1><%= @artist.name %></h1>

<h2><% @artist.songs.each do |song| %></h2>
  <a href="/songs/<%= song.slug %>"><%= song.name %></a>
  <% end %>

<h2><% @artist.genres.each do |genre| %></h2>
  <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
  <% end %>
