<h1><%=@artist.name%></h1>
<p>
<%@songs.each do |song|%>
  <li><a href='/songs/<%=song.slug%>'><%=song.name%></a><br>
    Genre:
    <%Song.find(song.id).genres.each do |genre|%>
      <a href='/genres/<%=genre.slug%>'><%=genre.name%></a>
    <% end %>
  </li>
<%end%>
</p>
