<h1>Artist: <%= @artist.name %></h1>
<ul>
  <li>
    <h3>Songs:</h3>
    <ul>
      <% @songs.each do |song| %>
        <li>
          <a href="/songs/<%= song.slug %>"><%= song.name %></a>
        </li>
      <% end %>
    </ul>
  </li>
  <li>
    <h3>Genres:</h3>
    <ul>
      <% # you need to ask about how to stop genres from being added to the artist for each new song with the same genre %>
      <% # you can get rid of the multiples on the page by using @genres.uniq.each instead of @genres.each %>
      <% @genres.uniq.each do |genre| %>
        <li>
          <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
        </li>
        
      <% end %>
    </ul>
  </li>
</ul>
