<h4>Artist: <%= @artist.name %></h1>
	<p>
		Songs: 
		<ul>
			<% @artist.songs.map do |song| %>
				<li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
			<% end %>
		</ul>
		<br>		
		Genre(s):
		<ul>
			<% @artist.genres.map do |genre| %>
				<li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
			<% end %>
		</ul>
	</p>