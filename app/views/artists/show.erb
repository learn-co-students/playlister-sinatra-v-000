<%= @sluggish.name %>
<% #binding.pry %>
<% @sluggish.songs.each do |song| %>
  <%= song.name %>
  <a href="/songs/<%= song.slug %>"></a>
  <% song.genres.each do |genre| %>
    <%= genre.name %>
    <a href="/genres/<%= genre.slug %>"></a>
  <% end %>
<% end %>
