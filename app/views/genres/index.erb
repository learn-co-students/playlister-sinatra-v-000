<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Genres List</title>
  <link rel="stylesheet" href="stylesheets/style.css" type="text/css">
 </head>
 <body>
  <h1> Genres </h1>
  <% if @genres.size > 0%>
    <ul>
      <% @genres.each do |genre| %>
        <li><a href="/genres/<%=genre.slug%>"><%=genre.name%></a><br></li>
      <% end %>
    </ul>
      <br><br><a href="/genres/new">Add A New Genre</a><br>
  <%else%>
  <h2>There are no genres to display!</h2>
  <%end%>
 </body>
</html>
