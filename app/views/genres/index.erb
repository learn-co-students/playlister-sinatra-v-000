<!DOCTYPE html>
<html>
 <head>
  <meta charset="UTF-8">
  <title>Genres Index Page</title>
 </head>
 <body>

  <h1>Genres</h1>
    <ol>
      <% @genres.each do |genre| %>
        <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
      <% end %>
    </ol>
  </body>
</html>
