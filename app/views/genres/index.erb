<h1> See all of the list of the genres below: </h1>
 <% @genres.all.each.with_index(1) do |genre, index| %>
   <p><a href = "/genres/<%=genre.slug %>"> <%= index%> <%= genre.name %> </a></p>
 <%end %>
