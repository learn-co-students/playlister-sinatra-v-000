<!DOCTYPE html>
<html>
<head>
  <title>All Genres</title>
</head>
<body>
  <h1>Genres</h1>
  <ul>
    <% @genres.each do |genre| %>
    <li><a href="/genres/<%=genre.slug%>"><%= genre.name %></a>
    <% end %>
  </ul>
</body>
</html>