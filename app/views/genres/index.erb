<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <ol>
    <% @genres.each do |s| %>
      <li><a href="/genres/<%= s.slug %>"><%= s.name %></a></li>
    <% end %>
    </ol>
  </body>
</html>
