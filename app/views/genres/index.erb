<!DOCTYPE html>
<html>
  <head>
    <title>Genre List</title>
  </head>
  <body>
    <h2>Click on an Genre for More Info:</h2>
    <% @genres.each do |genre| %>
    <a href="/genres/<%=genre.slug%>"><%=genre.name%></a>
    <% end %>
  </body>
</html>
