<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Genre List</title>
  </head>
  <body>
    <h1>Select Genre</h1>
    <ul>
      <% @genres.each do |genre| %>
      <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
      <% end %>
    </ul>
  </body>
</html>
