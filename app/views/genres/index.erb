<h1> List of Genres </h1>


  <% @genres.to_a.sort_by! {|genre| genre.name} %>
    <% @genres.each do |genre| %>
      <a href="/genres/<%= genre.slug %>"><%= genre.name.split(" ").collect {|word| word.capitalize}.join(" ") %></a><br>
  <% end %>