<!DOCTYPE html>
<html lang="en"> 
<head>
  <meta charset="UTF-8">
  <title>Genre Index</title>
</head>
<body>
  <h1>Genre List</h1>
    <ul>
      <% @genres.each do |genre| %>
      <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
      <% end %>
    </ul>
</body>
</html>