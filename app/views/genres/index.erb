<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <% @genres.each do |genre| %>
      <h1><%= genre.name %></h1>
      <h2><a href="/genres/<%=genre.slug%>">Link</a>
    <% end %>
  </body>
</html>
