<h1><%= @genre.name %></h1>

<p> Genres songs: 
    <ul> <% @genre.songs.all.each do |song| %>
        <li><a href="/songs/<%=song.slug%>"><%= song.name %></a></li> 
        <% end %>
    </ul>
</p>


<p> Genres artists: 
    <ul> <% @genre.artists.all.each do |artist| %>
        <li><a href="/artists/<%=artist.slug%>"><%= artist.name %></a></li> 
        <% end %>
    </ul>
</p>