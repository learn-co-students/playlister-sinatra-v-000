<h1><%= @genre.name %></h1>

<% @genre.songs.each do |song| %>
  Song: <a href="/songs/<%= song.slug %>"><%= song.name %><br>
<% end %>

<% @genre.artists.each do |artist| %>
  Artist: <a href="/artists/<%= artist.slug %>"><%= artist.name %><br>
<% end %>
