<h1> <%= @genre.name %> </h1>

<a href="/artists/<%= @genre.artist.slug %>"> <%= @genre.artist.name %> </a>

<% @genre.songs.each do |song| %>
  <a href="/songs/<%= song.slug %>"><%= song.name %> </a>
<% end %>
