<%= @genre.name %>
<% @genre.songs.all.each do |song| %>
<h4>Song/s: <a href='/songs/<%= song.slug %>'> <%= song.name %> </a> </h4>
<% end %>
<% @genre.songs.all.each do |song| %>

<h4>Artist/s: <a href='/artists/<%= song.artist.slug %>'> <%= song.artist.name %> </a> </h4>

<% end %>
