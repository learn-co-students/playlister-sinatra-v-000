<h1><%= @genre.name %></h1>

<h2>Genre's Song(s)</h2>

<ul>
  <% @genre.songs.each do |song| %>
    <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
    <ul><a href="/artists/<%= song.artist.slug %>"><%= song.artist.name %></a></ul>
    <br></br>
<% end %>
</ul>
