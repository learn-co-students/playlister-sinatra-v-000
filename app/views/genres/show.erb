<h2><%= @genre.name %></h2>

<h3>Artists: </h3>
  <% @genre.artists.each.with_index(1) do |artist, index| %>
    <p><a href = "/artists/<%= artist.slug %>"><%= artist.name %></a><p>
  <%end%>

<h3>Songs: </h3>
  <% @genre.songs.each.with_index(1) do |song, index| %>
    <p><%= index %>. <a href="/songs/<%= song.slug %>"> <%= song.name %></a><p>
    <%end%>
