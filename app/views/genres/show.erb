<h1>Genre Name: <%= @genre.name %></h1>
<% @genre.artists.each do |artist| %>
<h2>Artist Name(s): <a href="/artists/<%= artist.slug %>"><%= artist.name %></a></h2>
<% end %>
<% @genre.songs.each do |song| %>
<h2>Song Title(s): <a href="/songs/<%= song.slug %>"><%= song.name %></a></h2>
<% end %>