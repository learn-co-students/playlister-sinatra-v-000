<h1><%= @genre.name %></h1>

<h3>Songs:</h3>
<ul>
  <% @genre.songs.each do |song| %>
  <li><a href="/songs/<%= song.slug%>"><%= song.name %></a> (Artist: <a href="/artists/<%= song.artist.slug%>"><%= song.artist.name %></a>)</li>
  <% end %>
</ul>

