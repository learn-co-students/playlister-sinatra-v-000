<h1><%= @genre.name %></h1>
<h3>Genre Data</h3>
<ul>
  <li>Songs: <%= @genre.songs.map {|song| "<a href='/songs/#{song.slug}'>#{song.name}</a>"}.join(', ') %></li>
  <li>Genres: <%= @genre.artists.map {|artist| "<a href='/artists/#{artist.slug}'>#{artist.name}</a>"}.join(', ') %></li>
</ul>
