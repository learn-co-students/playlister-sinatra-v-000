<% @genre.artists.each do |artist| %>
  <%= artist.name %> <a href="/artists/<%= artist.slug %>">[link]</a>
<% end %>

<% @genre.songs.each do |song| %>
  <%= song.name %> <a href="/songs/<%= song.slug %>">[link]</a>
<% end %>