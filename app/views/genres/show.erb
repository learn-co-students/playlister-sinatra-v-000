<h2><%= @genre.name.capitalize  %></h2>
<p>Songs: <% @genre.songs.each do |song| %><a href="/songs/<%= song.slug %>"><%= song.name %></a> / <% end %></p>
<p>Artists: <% @genre.artists.each do |artist| %>
  <a href="/artists/<%= artist.slug %>"><%= artist.name %></a> / 
<% end %></p>
<a href="/songs">See a list of songs</a>
