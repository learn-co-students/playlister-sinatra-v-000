<h1><%= @genre.name %></h1>

<% @genre.songs.each do |song| %>
  <% artist = Artist.find_by(name: song.artist.name) %>
  <h3><a href="/songs/<%= song.slug%>"><%= song.name %> </a> by <a href="/artists/<%= artist.slug%>"><%= artist.name %> </a></h3>
<% end %>
