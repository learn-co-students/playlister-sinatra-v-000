Genre's Artists w/links
Genre's Songs w/links

<h1>Showing Genres</h1>

Genre Name: <%= @genre.name %>
Genre Songs: <% @genre.songs.each do |song| %>
  <a href="/songs/<%=song.slug%>"><%= song.name %></a><br>
  <% end %>
Genre Artists: <% @genre.artists.each do |artist| %>
  <a href="/artists/<%=artist.slug%>"><%= artist.name %></a><br>
  <% end %>