<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <h1><%= @genre.name %></h1>
    <h3>Artist </h3>
    <a href="/artists/<%= @artist.slug %>"><%= @artist.name %></a><br>
    <h3>Song</h3>
    <a href="/songs/<%= @song.slug %>"><%= @song.name %></a>
    <a href="/songs">HOME</a>
  </body>
</html>
