<html>
  <head>
    <title>Selected Genre</title>
  </head>

  <body>
    <h1> <%= @genre.name %> </h1>

    <h4>Songs:</h4>
    <% @genre.songs.each do |song| %>
        <ul><a href= '/songs/<%= song.slug %>'> <%= song.name %> </a></ul>
      <% end %>

      <h4>Artists:</h4>
      <% @genre.artists.each do |artist| %>
          <ul><a href= '/artists/<%= artist.slug %>'> <%= artist.name %> </a></ul>
        <% end %>


  </body>

</html>
