<h1>Genre: <%=@genre.name%></h1>

<h1>Artist(s): <%@genre.artists.each do |artist|%>
  <a href="/artists/<%=artist.slug %>"><%= artist.name %></a></h1>
  <%end%>
  <h1>Song(s): <%@genre.songs.each do |song|%>
  <a href="/songs/<%=song.slug %>"><%= song.name %></a></h1>
  <%end%>

