<!DOCTYPE HTML>
<html>
  <head>
    <title>The Artists' Genres</title>
  </head>
  <body>
    <h1><%=@genre.name%></h1>
    <h3>List of Artists</h3>
    <ul>
      <%@genre.artists.each do |artist|%>
        <li><a href="/artists/<%= artist.slug %>"><%=artist.name%></a></li>
      <%end%>
    </ul>
    <br>
    <h3>List of Songs</h3>
    <ul>
      <%@genre.songs.each do |song|%>
        <li><a href="/songs/<%= song.slug %>"><%=song.name%></a></li>
      <%end%>
    </ul>
  </body>
</html>
