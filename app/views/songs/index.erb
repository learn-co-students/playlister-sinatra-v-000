<!DOCTYPE html>
<html>
  <head>
    <title>Playlister</title>
  </head>

  <body>
    <ol>
    <% @songs.each do |s| %>
      <h3><li><a href="/songs/<%= s.slug %>"><%= s.name %></a></h3>
        <a href="/songs/<%= s.slug %>/edit"> Edit Song</a></li>
    <% end %>
    </ol>
  </body>
</html>
