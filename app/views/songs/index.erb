<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show Songs</title>
  </head>
  <body>
    <form action="/songs" method="post">
      <% @songs.each do |song| %>
      <a href="/songs/<%= song.slug %>"> <%= song.name %> </a><br>
      <% end %>
    </form>
  </body>
</html>
