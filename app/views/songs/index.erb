<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>Songs</title>
    </head>
    <body>
        <h1>Songs</h1>
        <p>
          <ul>
            <% @songs.each do |song| %>
              <li>
                <a href="/songs/<%= song.slug %>"><%= song.name %></a>
              </li>
            <% end %>
          </ul>
        </p>
    </body>
</html>