<h1>All Songs In Playlister</h1>
<% @songs.each do |song| %>
<h2>Name: <a href="/songs/<%= song.slug %>"><%= song.name %></a></h2>
<p>Artist: <%= song.artist.name %><br>
Genres: <%= Genre.find(SongGenre.find_by(song_id: song.id).genre_id).name %></p>
<% end %>