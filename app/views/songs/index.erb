<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Songs</h1>

    <% @songs.each do |song| %>
      <p><a href="/songs/<%= song.slug %>"><%= song.name %></a></p>
    <% end %>
  </body>
</html>
