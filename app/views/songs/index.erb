<% if !@songs.blank? %>
  <h1>Songs</h1>
  <ol>
    <% @songs.each do |song|  %>
    <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
    <% end %>
  </ol>
<% else %>
  <h2>You have no songs yet.</h2>
<% end %>
