<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h2>Song index</h2>
    <ul>
      <% Song.all.each do |song| %>
        <li><a href="/songs/<%= song.slug %>"><%= song.name %></a></li>
      <% end %>
    </ul>
  </body>
</html>
