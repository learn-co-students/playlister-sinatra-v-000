<ul>
  <% if !@songs.empty? %>
    <% @songs.each do |song|%>
    <li><a href='/songs/<%= "#{song.slug}" %>'><%= song.name %></a></li>
    <%end%>
    <%else%>
    <li>We need some songs here!</li>
  <%end%>
</ul>
