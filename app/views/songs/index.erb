<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>You have hit the index page</h1>
    <% @songs.each do |song| %>
      <a href="/songs/<%= song.slug %>"><%= song.name %></a>
    <% end %>
  </body>
</html>
