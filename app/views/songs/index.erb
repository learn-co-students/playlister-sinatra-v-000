<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Song Index</title>
  </head>
  <body>
    <h1>Songs</h1>
    <% @songs.each do |song|%>
      <a href='/songs/<%= song.slug %>'> <%= song.name %></a>
    <% end %>
  </body>
</html>
