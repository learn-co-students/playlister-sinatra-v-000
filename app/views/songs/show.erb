<!DOCTYPE html>
<html lang="en"> 
<head>
  <meta charset="UTF-8">
  <title></title>
</head>
<body>
  <h1><%= @song.name %></h1>
  <h3>Artist: <a href="/artists/<%= @song.artist.slug %>"><%= @song.artist.name %></h3>
  
    <h3><label> Genre: </label>
    <% @song.genres.each do |genre| %>
  <a href="/genres/<%= genre.slug %>"><%= genre.name %></a></h3>
    <% end %>
 

  
<% if flash.has?(:message) %>
  <%= flash[:message] %>
  <% end %>
</body>
</html>

