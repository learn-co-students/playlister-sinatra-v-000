<!DOCTYPE html>
<html>
  <head>Songs Index</head>
  <body>
    <% if flash.has? :message %>
      <%= flash[:message] %>
    <% end %>

    <%= @song.name %>
    <a href="/artists/<%=@song.artist.slug%>"><%= @song.artist.name %></a>
    <% @song.genres.each do |genre| %>
      <a href="/genres/<%=genre.slug %>"><%= genre.name %></a>
    <% end %>
    <p>Artist count: <%= Artist.count %></p>
  </body>
</html>