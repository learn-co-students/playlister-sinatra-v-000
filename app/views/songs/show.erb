<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Show <%= @song.name %></title>
</head>
<body>
  <% unless locals.empty? %>
    <%= message %>
  <% end %>
  <h3> <%= @song.name %> </h3>
  <br>
  <a href="/artists/<%= @song.artist.slug %>"> <%= @song.artist.name %> </a>
  <% @song.genres.each do |genre| %>
    <a href="/genres/<%= genre.slug %>"><%= genre.name %> </a>
  <% end %>
</body>
</html>