<!-- 
Any given song's show page should have links to that song's artist and the each genre associated with the song. 
Takes in an instance variable @song.
-->

<%= flash[:message] if flash.has?(:message) %>

<h2><%= @song.name %>:</h2>

<h3>Artist: <a href="/artists/<%= @artist.slug %>"><%= @artist.name %></a></h3>

<h3>Genres:</h3>

<ol>
  <% @genres.each do |genre| %>
    <li><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></li>
  <% end %>
</ol>