<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show Page</title>
  </head>
  <body>
     <h2><%=@slug_song.name%></h2>
     <h3><a href = "/artists/<%=@slug_song.artist.slug%>"><%=@slug_song.artist.name%></a></h3>
     <h3><a href = "/genres/<%=@slug_song.genres[0].slug%>"><%=@slug_song.genres[0].name%></h3></a>
  </body>
</html>
