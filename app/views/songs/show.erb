<h1><%= @song.name %></h1>

<h2>Artist:</h2>
  <a href='/artists/<%= @artist.slug %>'>
    <%= @artist.name %>
  </a>

<h3>Genres:</h3>
  <% @genres.each do |genre| %>
    <a href= '/genres/<%= genre.slug %>'>
      <%= genre.name %>
    </a>
  <% end %>
