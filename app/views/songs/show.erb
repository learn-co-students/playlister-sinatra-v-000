<h1><%= @notice %></h1>

<h1><%= @song.name %></h1>

<h3>By:<a href=<%= "/artists/#{@song.artist.slug}"%>> <%= @song.artist.name %></h3></a>

<h4>Genres: </h4>

<% @genres.each do |genre| %>
  <a href=<%= "/genres/#{genre.slug}"%>><%= genre.name %></a>
<% end %>
