<!DOCTYPE html>
<html lang="en" dir="ltr">
  <body>
    <%= @song.name %><br>
    <%= @song.artist.name %><br>
    <% @song.genres.each do |genre| %>
    <%= genre.name %>
    <% end %>
  </body>
</html>
