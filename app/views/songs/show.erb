
    <% @songs.each do |obj| %>

    <h1> <%= obj.name %> </h1>
    <ul>  Artist:
    <a href="/artists/<%=obj.artist.slug%>">  <%= obj.artist.name %> </a> </ul>
    <ul>Genres: <% obj.genres.each do |genre| %> </ul>
          <li> <a href="/genres/<%= genre.slug%>">  <%= genre.name %> </a> </li>

        <% end %>
      <% end %>
   <br></br>
