<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Show Page</title>
  </head>
  <body>
    <% if flash.has?(:message) %>
      <%= flash[:message] %>
    <% end %>
     <h2><%=@slug_song.name%></h2>
      <%if @slug_song.artist%>
        <a href = "/artists/<%=@slug_song.artist.slug%>"><%= @slug_song.artist.name %></a>
      <%end%>
    <ul>
     <%@slug_song.genres.each do |genre|%>
          <li><a href = "/genres/<%=genre.slug%>"><%=genre.name%></a></li>
     <%end%>
   </ul>
  </body>
</html>
