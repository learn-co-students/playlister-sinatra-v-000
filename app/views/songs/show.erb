<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <%= flash[:message] if flash.has?(:message) %>
  
  <%= @song.name %> - <a href="/artists/<%= @song.artist.slug %>"><%=@song.artist.name%></a></h4>

  <% @song.genres.each do |genre| %>
    <a href="/genres/<%= genre.slug %>"><%= genre.name %></a>
  <% end %>

</body>
</html>