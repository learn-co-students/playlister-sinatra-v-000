<% if flash.has?(:message) %>
  <%= flash[:message] %>
<% end %>

<h1><%=@song.name%></h1>
<label>Artist:</label>
<ul>
  <p><a href="/artists/<%= @song.artist.slug %>"><%= @song.artist.name %></a></p>
</ul>

<label>Genres:</label>
<ul>
  <% @song.genres.each do |genre| %></h1>
    <p><a href="/genres/<%= genre.slug %>"><%= genre.name %></a></p>
  <% end %>
</ul>
