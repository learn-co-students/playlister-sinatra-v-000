<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Edit a Song</title>
  </head>
  <body>
    <p>Edit a song:</p>
    <form action="/songs/<%= @song.slug %>" method="post">
      <input name="_method" type="hidden" value="patch">
      <p>
        <label name="song[name]">Name: </label>
        <input type="text" name="song[name]" id="Name" value="<%=@song.name%>">
      </p>

      <p>
        <label for="artist[name]">Artist: </label>
        <input type="text" name="artist[name]" id="Artist Name" value="<%=@song.artist.name%>">
      </p>
      <p>
        <label for="song[genre_ids][]">Genres:</label>

        <% @genres.each do |genre| %>
          <% checked = @song.genres.include?(genre) ? "checked" : "" %>
          <input id="<%= genre.name %>" type="checkbox" name="song[genre_ids][]" value="<%= genre.id %>" checked><%= %><%genre.name%></input>
        <%end%>

      </p>
      <p>
        <input type="submit" value="Save">
      </p>
  </form>

  </body>
</html>
