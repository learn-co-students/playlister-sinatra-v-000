<!DOCTYPE html>
<html>
  <head>
    <title>Edit Song</title>
  </head>
  <body>
    <h1>Edit a song</h1>
    <form action="/songs/<%=@song.slug%>" method="post">
      <input id="hidden" type="hidden" name="_method" value="patch">
      <p>Name: <input type="text" name="song[name]" id="Name" value="<%=@song.name%>"></input></p>
      <h2> Genre(s): </h2>
      <% @genres.each do |genre| %>
        <p><input type="checkbox" name="song[genre_ids][]" value="<%=genre.id%>" id="<%=genre.name%>" <%='checked' if @song.genre_ids.include?(genre.id) %>><%= genre.name%></input></p>
      <% end %>
      <p><input type="text" name="artist_name" id="Artist Name">Artist Name</input></p>
      <input type="submit" value="Save"></input>
    </form>
  </body>
</html>