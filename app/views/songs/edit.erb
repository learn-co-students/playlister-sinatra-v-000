
<h1>Edit a Song</h1>
<form action="/songs/<%=@song.slug%>" method="post">
	<p>Name: <input type="text" name="song[name]" id="Name" value="<%=@song.name%>"></p>
	<p>Choose genres:</p>
	<%Genre.all.each do |genre|%>
	<input type="checkbox" name="song[genre_ids][]" value="<%=genre.id%>" id="<%=genre.name%>" <%='checked' if @song.genres.include?(genre)%>><%=genre.name%></input>
	<%end%>
	<p>Choose an existing artist:</p>
	<%Artist.all.each do |artist|%>
	<input type="checkbox" name="song[artist_id]" value="<%=artist.id%>" id="<%=artist.name%>" <%='checked' if @song.artist == artist%>><%=artist.name%></input>
	<%end%>
	</input>
	<p>or create a new one:</p>
	<p>Artist Name: <input type="text" name="artist[name]" id="Artist Name"></p>
	<button value="submit">Save</button>
</form>