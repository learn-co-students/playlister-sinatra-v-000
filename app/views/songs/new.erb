<h2>Add New Song</h2>

<br>

<form action="/songs" method="POST">
  Name: <input type="text" name="song[name]" id="Name"></input>
  
  <br></br>
  
  Artist: <input type="text" name="artist[name]" id="Artist Name"></input>
  
  <br></br>

  Genre(s): <% Genre.all.each do |genre| %>

    <input type="checkbox" name="genres[]" id="<%= genre.name %>" value="<%= genre.id %>"> <%= genre.name %> </input>
  <% end %>

  <br></br>
  
  <input type="submit" value="Create">
</form>
