<!DOCTYPE html>
<html>
<head>
  <title>New Song</title>
</head>
<body>
  <h1>Create a New Song</h1>
  <h3>Instructions</h3>
  <ul>
    <li>Give the Song a Name</li>
    <li>Give the Song an Artist</li>
    <li>Give the Song a Genre</li>
  </ul>

  <form action="/songs" method="POST">
    <label><h3>Give the Song a Name</h3></label>
    <input type="text" name="Name" id="song_name">


    <label><h4>Choose an existing Artist:</h4></label>
    <%Artist.all.each do |artist|%>
      <input type="checkbox" name="existing_artist" value="<%=artist.id%>"><%=artist.name%></input>
    <%end%>
  
    <label><h4>and/or, create a new artist:</h4></label>
      <input  type="text" name="Artist Name"></input>
    <br></br>

    <label><h3>Give the Song a Genre</h3></label>
      <%Genre.all.each do |genre|%>
      <input type="checkbox" name="genre" value="<%=genre.id%>" id="<%=genre.name%>"><%=genre.name%></input>
      <%end%>
    <br></br>
    <input type="submit" value="Create">
  </form>

</body>
</html>
