<!DOCTYPE html>
  <head>
    <meta charset="utf-8" />
    <title>Create New Song</title>
  </head>
  <body>
    <form action="/songs" method="POST">
      <h1>Create a New Song</h1>
      <p>
        <label>Name</label>
        <input type="text" name="song_name" id="Name"></input>
      </p>

      <p>
        <label>Find or enter artist</label>
        <input type="text" name="artist_name" id="Artist Name"></input>
      </p>

      <p>
      <label>Genres</label>
      <ul>
      <% @genres.each do |genre| %>
       <li> <input type="checkbox" id="<%= genre.name %>" name="genres[]" value="<%= genre.id %>"><%= genre.name %></input></li>
      <% end %>
      </ul>
      </p>
      <input type="submit" value="Create">
    </form>
  </body>
</html>
