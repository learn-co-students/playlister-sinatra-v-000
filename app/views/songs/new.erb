<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Song</title>
    <h1>Create a New Song!</h1>
  </head>
  <body>
    <form action="/songs" method="post">
      <label>Song Name:</label>
      <input type="text" name="song[name]" value="New Name">
      <br>
      <label>Choose an existing artist...</label>
      <br>
      <% Artist.all each do |artist| %>
        <input type="checkbox" name="song[artist_id][]" value="<%= artist.slug %>" id="<%= artist.id"><%= artist.name %></input>
      <% end %>
      <label>and/or create a new artist:</label>
      <br>
      <label>Artist Name:</label>
      <input type="text" name="artist[name]">
      <br>
      <input type="submit" value="Create Song">
    </form>
  </body>
</html>
