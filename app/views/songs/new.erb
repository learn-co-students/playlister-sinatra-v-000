<h1>Add a New Song</h1>

 <form action="/songs" method="POST"> 
  <label>Name:</label> 

  <br></br> 

  <input type="text" name="song[name]" id="song_name"> 

  <input type="submit" value="Create Song"> 
 </form> 

 <%@genres.each do |genre|%>
   <input type="checkbox" name="genre[genre_ids][]" value="<%=genre.id%>" id="<%=genre.id%>"><%=gen4.name%></input>
<%end%>

 
<!--<%@songs.each do |song|%>
   <input type="checkbox" name="song[name]" value="<%= song.id %>" id="<%= song.id %>"><%= song.name %></input>
<%end%> -->
