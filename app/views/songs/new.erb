<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>New Song</title>
    <h1>Create a New Song!</h1>
  </head>
  <body>
    <form action="/songs" method="post">
      <label>Name:</label>
      <input type="text" name="song[name]" id="Name">
      <br></br>
      <label>Choose an existing artist:</label>
      <br></br>
      <% Artist.all.each do |artist| %>
        <p><input type="checkbox" name="song[artist_id][]" value="<%= artist.id %>" id="<% artist.name %>"><%= artist.name %></input></p>
      <% end %>
      <br></br>
      <label>Or create or new one:</label>
      <br></br>
      <label>Name:</label>
        <input type="text" name="artist[name]" value="Artist Name">
      <br></br>
      <input type="submit" value="Create Song">
    </form>
  </body>
</html>
