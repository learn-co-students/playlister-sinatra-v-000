<!DOCTYPE html> 
<html>
  <head>Songs Index</head>
  <body>
  <h1>Song List</h1>
    <ul>
      <% @songs.each do |song| %>
        <li><a href= "/songs/<%=song.slug%>"><%= song.name %></a></li>
      <% end %>
    </ul>
  </body>
</html>